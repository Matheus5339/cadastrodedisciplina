# Decisões

| ID | Decisão | Origem | Justificativa | Impacto | Data |
| :--- | :--- | :--- | :--- | :--- | :--- |
| D1 | Backend em **.NET 10** (C#) | `instrucoes.txt` §4 ("corenet 10") | Fonte normativa superior prevalece sobre README/CLAUDE.md antigos que citam .NET 9 | Exige instalação do SDK .NET 10 (pendência P1) | 2026-06-11 |
| D2 | Persistência com **EF Core + SQLite + migrations** | README original + `leia.txt` §10 | Compatível com `instrucoes.txt` §7; Dapper da implementação antiga não será copiado | Migrations versionadas; banco criado em runtime, nunca versionado | 2026-06-11 |
| D3 | Importação de CSV com **caminho configurável** (`CsvImport:Path`), nome padrão `disciplinas.csv`, aliases aceitos: `discipolinas.csv` (typo do normativo) e `disciplinas-ecomp-ucp.csv` (arquivo real existente) | Divergência entre `instrucoes.txt` §2 e arquivos reais | O normativo contém erro de grafia e o arquivo real tem outro nome; aliases documentados evitam falha silenciosa | Importador procura, na ordem: caminho configurado → `disciplinas.csv` → aliases | 2026-06-11 |
| D4 | Fórmula do CR: `CR = soma(mediaFinal × creditos) / soma(creditos)`, arredondado a 2 casas decimais — **CONFIRMADA pelo usuário em 2026-06-12** (deixou de ser hipótese) | Decisão humana (resposta "3a") | Média ponderada pelos créditos é a definição acadêmica usual de coeficiente de rendimento | Implementação e testes seguem esta fórmula | 2026-06-12 |
| D5 | Frontend como projeto Vite em `src/Frontend/`, com a pasta interna `src/` seguindo **exatamente** a árvore de `instrucoes.txt` §5; backend em `src/Backend/{Api,Application,Domain,Infrastructure,Shared,Tests}` | `instrucoes.txt` §5/§6 + `leia.txt` §10 | O normativo descreve as duas árvores a partir de `src/`; o repositório único exige separar em `src/Frontend` e `src/Backend` | Estruturas físicas obrigatórias preservadas dentro de cada lado | 2026-06-11 |
| D6 | Entidade `Aluno` ganha `passwordHash` e campos técnicos mínimos de autenticação (ex.: `createdAt`) | `leia.txt` §11; autenticação exigida | `instrucoes.txt` modela aluno sem senha, mas exige controle de acesso — sem hash de senha não há login | Modelo de dados estendido de forma documentada | 2026-06-11 |
| D7 | Tabelas técnicas adicionais: `RefreshTokens` (rotação/revogação) e `ImportLog` (controle de importação única do CSV) | `leia.txt` §11/§12/§15 | Refresh token seguro exige persistência e revogação; importação idempotente exige registro de execução | Duas tabelas a mais, documentadas | 2026-06-11 |
| D8 | Senhas com **Argon2id**; MD5 e SHA-256 simples proibidos; SHA-256 permitido **somente** para hash de refresh token (não é senha) | `leia.txt` §12; aprendizado da implementação antiga | Argon2id é o padrão recomendado para senhas; hash rápido para token opaco de uso único é prática aceita | Hook de governança bloqueia MD5/SHA-256 em contexto de senha | 2026-06-11 |
| D9 | Exceção única de escrita externa: append em `../rastreamento.md`, exigido pelo `CLAUDE.md` do diretório pai | Conflito `leia.txt` §6 vs `CLAUDE.md` externo | O projeto pai mantém rastreamento obrigatório de toda interação; registrar lá não altera referências | Hook permite apenas esse arquivo externo | 2026-06-11 |
| D10 | `Disciplina` é **catálogo global compartilhado**; o isolamento por aluno aplica-se a histórico, perfil e foto | Interpretação de `instrucoes.txt` §2 | A importação de CSV ocorre na inicialização (sem usuário autenticado) e atualiza "a tabela disciplinas" com código único — só é coerente como catálogo global; "suas disciplinas" = disciplinas **cursadas** (histórico), que é integralmente isolado por aluno via token | CRUD de disciplinas exige autenticação; histórico/CR/foto são 100% escopados ao aluno do token | 2026-06-12 |
| D11 | Validações com DataAnnotations nos contracts + validações de regra nos services | `instrucoes.txt` §6 ("FluentValidation ou equivalente") | "Ou equivalente" permite mecanismo nativo; menos dependências externas | Pasta `Application/Validators` concentra validações reutilizáveis | 2026-06-12 |
| D12 | Bibliotecas: `Isopoh.Cryptography.Argon2` (Argon2id), `Microsoft.AspNetCore.Authentication.JwtBearer`, `Swashbuckle.AspNetCore` (Swagger dev), `xunit` + `Microsoft.AspNetCore.Mvc.Testing` (testes) | Ecossistema padrão .NET | Bibliotecas maduras e auditadas; escolha própria (não cópia — coincidência com o antigo é pelo padrão de mercado) | Registradas nos `.csproj` | 2026-06-12 |
| D13 | Validação do frontend via `tsc --noEmit` + execução dev; **nenhum `vite build`/`dist` gerado** | `leia.txt` §18 (entrega só código-fonte) + regra do hook | Gerar build de entrega é proibido; checagem de tipos valida o código sem produzir artefatos | `dist/` nunca existe no projeto novo | 2026-06-12 |
| D14 | Pendências resolvidas pelo usuário em 2026-06-12: prosseguir **sem** os documentos do professor (2a) e hook ativado abrindo sessões na pasta nova (4a) | Decisão humana | Documentos inexistem no diretório; opção (a) evita escrita fora da pasta nova | P2 e P6 encerradas | 2026-06-12 |
| D15 | Importadas as disciplinas dos **7 cursos do CEC** (Computação já existente + Civil, Produção, Elétrica, Mecânica, Mecatrônica, Arquitetura) a partir das matrizes públicas em `ucp.br`. Premissas: (a) **créditos = carga horária ÷ 18** (1 crédito = 18h, padrão do CSV de Computação: 72h→4, 36h→2, 198h→11); (b) **professor em branco** (a UCP não publica o docente por disciplina); (c) **código gerado** no padrão `PREFIXO-NNN` por curso (CIV, PROD, ELET, MEC, MECT, ARQ); (d) disciplinas que se repetem entre cursos são **entradas distintas por curso** (código é a chave única; o período varia por curso) | Decisão humana (usuário pediu "todos os cursos"; após análise, escopo acordado = CEC) e dados públicos limitados | A UCP só publica nome, período e carga horária; código e professor não existem publicamente e **não podem ser inventados** (REGRAS §8) — por isso ficam gerados/em branco de forma transparente. Catálogo é global (ver D10), então manter entradas por curso preserva o período correto de cada matriz | Catálogo passou de 72 → **498 disciplinas**; arquivo gerado em `src/Backend/Api/data/disciplinas.csv` por `scripts/gerar_disciplinas_cec.py`; `CsvImport:Path` (dev) aponta para esse arquivo; import idempotente (426 novas, 72 já existentes) | 2026-06-13 |
